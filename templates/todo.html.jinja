<!DOCTYPE html>

<head>
    <title>Todo Page</title>
    <style>
        body {
            background-color: #505050;
        }

        h1 {
            color: white;
            font-weight: bolder;
            text-align: center;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        .completed-li {
            background-color: #4CBB17;
        }

        .completed-li  input[type='submit']:hover {
             background-color: #336E11;
        }
        li {
            background-color: #A31E39;
            padding: 15px;
            margin-bottom: 20px;
            margin-left: auto;
            margin-right: auto;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            border-radius: 20px;
            max-width: 600px;
            text-align: center;
            font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
        }

        input[name='new_todo'] {
            padding: 15px;
            border: 2px solid #ddd;
            border-radius: 8px;
            width: 200px;
            margin-right: 20px;
        }

        input[type='submit'] {
            background-color: transparent;
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            display: block;
        }


        input[type='submit']:hover {
            background-color: #761329;
        }


        form {
            display: inline-block;
        }
    </style>
</head>

<body>
    <h1>My To-Do List</h1>

    <ul>
        {% for todo in todos %}
        <li {% if todo['completion']==1 %}class="completed-li" {% endif %}>
            {{ todo['description'] }}

            <div class="button">
                <form method="POST" action="/delete_todo/{{ todo.id }}">
                    <input type="Submit" value="Delete">
                </form>

                <form method="POST" action="/complete_todo/{{ todo.id }}">
                    <input type="Submit" value="Complete">
                </form>
            </div>
        </li>
        {% endfor %}
    </ul>

    <form method="post">
        <input name="new_todo">
        <input type="submit">
    </form>

</body>

</html>